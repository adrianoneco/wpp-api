# Environment Configuration
NODE_ENV=development

# Server Configuration
HOST=0.0.0.0
PORT=3000

# MongoDB Configuration
MONGODB_URI=mongodb://mongodb:27017/wpp-api

# MinIO S3 Configuration
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=wpp-media
MINIO_USE_SSL=false

# WPPConnect Configuration
WPP_SESSION_NAME=wpp-session
WPP_HEADLESS=true

# Fixed Instance Mode (optional)
# Set INSTANCE_NAME to automatically create a session on startup
# Example: INSTANCE_NAME=suporte
INSTANCE_NAME=
